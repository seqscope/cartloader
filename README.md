# cartloader

## Installation

```bash
## install cartloader locally
git clone --recursive https://github.com/seqscope/cartloader.git
cd cartloader
pip install -e .

## install submodules
cd submodules
bash -x build.sh
cd ..
```
